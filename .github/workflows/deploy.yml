name: Deploy tl-showroom-frontend-poc

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Desplegar en el servidor
        run: ssh root@equalitech.xyz "cd /opt/frontend/showroom-frontend && git pull origin main && ./deploy.sh"
