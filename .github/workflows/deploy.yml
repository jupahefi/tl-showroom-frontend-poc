<<<<<<< HEAD
name: Deploy tl-showroom-frontend-poc
=======
name: Deploy Application
>>>>>>> 6bf59f5 (Initial commit)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
<<<<<<< HEAD
        uses: actions/checkout@v4

      - name: Desplegar en el servidor
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          echo "$SSH_PRIVATE_KEY" > private_key && chmod 600 private_key
          ssh -o StrictHostKeyChecking=no -i private_key usuario@tu-servidor "cd /opt/frontend/showroom-frontend && git pull origin main && ./deploy.sh"
=======
        uses: actions/checkout@v3

      - name: Configurar SSH
        run: |
          echo "$SSH_PRIVATE_KEY" > private_key
          chmod 600 private_key
          mkdir -p ~/.ssh
          mv private_key ~/.ssh/id_rsa

      - name: Deploy en servidor
        run: |
          ssh -i ~/.ssh/id_rsa root@tl-showroom.equalitech.xyz << 'EOF'
          cd /opt/easyengine/sites/tl-showroom.equalitech.xyz/app/backend
          git pull origin main
          docker-compose up -d --build
          EOF
>>>>>>> 6bf59f5 (Initial commit)
